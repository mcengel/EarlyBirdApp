<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:local="clr-namespace:EarlyBirdApp.ViewModels"
             x:Class="EarlyBirdApp.Views.StartGame2">
    <ContentPage.BindingContext>
        <local:StartGameViewModel />
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <Label Text="{Binding Name}"/>
            <Label Text="{Binding DisplayName}"/>
            <Label Text="Page 2" />
            <Entry Text="I am an Entry" />
            <Button Text="Go to Page 3" Clicked="GoToNextPage" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>